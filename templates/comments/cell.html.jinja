{% from "_macros.html.jinja" import link, send_trait, sync_trait -%}

<span class='code interior-mut'>Cell</span>
({% call link('https://doc.rust-lang.org/std/cell/struct.Cell.html', 'code') -%}
    std::cell::<span class='interior-mut'>Cell</span>
{%- endcall %}) is a memory container that provides <span class='interior-mut'>interior mutability</span>. This type is <em>not</em>
thread-safe ({{ send_trait(true) }} and {{ sync_trait(true) }}). Data can be moved orcopied in and out of the container, but you
cannot borrow the inner data. Since <span class='code interior-mut'>Cell</span>s are not thread-safe and do not allow borrowing of
the data, they remain safe without the need for additional runtime checks.
